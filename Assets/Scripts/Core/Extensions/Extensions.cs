using UnityEngine;

public static partial class Extensions
{
	/* public static T Instantiate<T>(this T obj, T original, Vector3 position = default, Quaternion rotation = default, Transform parent = null) where T : Object
	{
		return Object.Instantiate(obj, position, rotation, parent);
	} */
}